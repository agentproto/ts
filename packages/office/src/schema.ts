/**
 * AIP-22 OFFICE.md frontmatter zod schema.
 *
 * Generated from `resources/aip-22/draft/OFFICE.schema.json` via
 * json-schema-to-zod. Imported by both `define-office.ts` (TS path
 * validation) and `manifest/index.ts` (.md path validation) so every
 * field-level constraint runs in both authoring paths from a single
 * source of truth — re-run scaffold-aip to refresh after spec changes.
 *
 * Cross-field rules (if/then/allOf in JSON Schema) don't translate
 * cleanly and live in `define-office.ts`'s `validate(def)` instead.
 */

import { z } from "zod"

export const officeFrontmatterSchema = z.object({ "schema": z.literal("office.workspace/v1").describe("Discriminator for the AIP-22 workspace doctype."), "name": z.string().regex(new RegExp("^[a-z][a-z0-9-]*[a-z0-9]$")).min(2).max(96).describe("Stable kebab-case identifier for the company or view."), "title": z.string().min(1).max(200).describe("Human-readable company title."), "description": z.string().min(1).max(2000).describe("One-paragraph statement of purpose: what this company is, who it serves."), "version": z.string().regex(new RegExp("^[0-9]+\\.[0-9]+\\.[0-9]+(-[A-Za-z0-9.-]+)?$")).describe("Semantic version of the WORKSPACE shape. Bump on collection / orgTree / lint / defaults changes. Independent of the company's content version."), "extends": z.string().regex(new RegExp("^(\\.\\./|\\./)[^\\s]+/COMPANY\\.md$")).min(1).max(512).describe("OPTIONAL — relative path to a parent OFFICE.md. Presence makes the manifest a VIEW; absence makes it a WORKSPACE ROOT. Recursive composition; maximum chain depth is 8.").optional(), "appliesTo": z.array(z.string().regex(new RegExp("^(ws://(operators|skills)/[a-z][a-z0-9-]*|\\.\\./[^\\s]+)$")).describe("Either a ws:// ref to an AIP-9 operator or AIP-3 skill, or a relative path to a consumer workspace folder.")).refine((arr) => arr.every((item, i) => arr.indexOf(item) == i), { message: "All items must be unique!" }).describe("OPTIONAL — list of consumers this VIEW adapts the company for. Hosts MUST refuse the view if any binding does not resolve. Not inherited; views declare their own scope.").optional(), "identity": z.object({ "legalName": z.string().min(1).max(200).describe("OPTIONAL — registered legal name of the entity, if it differs from the title.").optional(), "legalEntity": z.string().regex(new RegExp("^ws://companies/[a-z][a-z0-9-]*$")).describe("OPTIONAL — ws:// ref to the legal entity OFFICE.md (self-ref if this manifest IS the legal entity; parent ref if this is a subsidiary view).").optional(), "jurisdiction": z.string().regex(new RegExp("^[A-Z]{2}$")).describe("OPTIONAL — ISO 3166-1 alpha-2 country code for primary jurisdiction (e.g. US, FR, DE, GB).").optional(), "foundedAt": z.string().regex(new RegExp("^[0-9]{4}-[0-9]{2}-[0-9]{2}$")).describe("OPTIONAL — ISO 8601 date the entity was founded.").optional(), "mission": z.string().min(1).max(2000).describe("OPTIONAL — one-paragraph mission statement.").optional(), "defaultCurrency": z.string().regex(new RegExp("^[A-Z]{3}$")).describe("OPTIONAL — ISO 4217 default currency code (e.g. USD, EUR, GBP).").optional(), "taxId": z.string().min(1).max(64).describe("OPTIONAL — tax / VAT identifier.").optional() }).strict().describe("Organisation identity fields. Each leaf field independently overridable via merge; a divisional view MAY narrow `jurisdiction` while inheriting `mission` from the parent.").optional(), "executor": z.string().regex(new RegExp("^ws://operators/[a-z][a-z0-9-]*$")).describe("OPTIONAL — AIP-9 default org-level operator. The host activates this operator for company-level prompts when no more-specific operator applies.").optional(), "governance": z.string().min(1).max(512).describe("OPTIONAL — AIP-7 policy or audit binding. May be a path to an AIP-7 policy file or a ws:// ref. Org-level approvals (role creation, reporting reassignment, jurisdiction change) flow through this ref. Subject to the office_signing_downgrade one-way switch when the bound policy declares signing.required=true.").optional(), "work": z.string().regex(new RegExp("^ws://workspaces/[a-z][a-z0-9-]*$")).describe("OPTIONAL — AIP-20 work tracker the company runs. Cross-references on items resolve against the bound work workspace by default.").optional(), "agency": z.string().regex(new RegExp("^ws://agencies/[a-z][a-z0-9-]*$")).describe("OPTIONAL — AIP-21 agency context. Set when the company also operates as a commercial agency selling services to external counterparties.").optional(), "knowledge": z.string().regex(new RegExp("^ws://wikis/[a-z][a-z0-9-]*(/KNOWLEDGE\\.md)?$")).describe("OPTIONAL — AIP-10 KNOWLEDGE.md ref. The institutional wiki — runbooks, onboarding docs, decision logs.").optional(), "playbook": z.string().regex(new RegExp("^ws://playbooks/[a-z][a-z0-9-]*$")).describe("OPTIONAL — AIP-12 active playbook governing culture, values, and operating rhythm.").optional(), "collections": z.array(z.any()).describe("Collections enabled by this organisation. Three forms supported: inline (full COLLECTION.md frontmatter), file ref, or registry import. Merge-by-effective-name (alias if set, otherwise the collection's name) across the extends chain.").default([] as never), "orgTree": z.object({ "containment": z.object({ "enabled": z.boolean().describe("Whether org-tree containment is active. ONE-WAY SWITCH: once true at any ancestor, descendants MUST NOT set false (would orphan items). Refusal: office_orgtree_disable (HARD).").default(true), "field": z.string().regex(new RegExp("^[a-z][a-zA-Z0-9_]*$")).describe("Item field name carrying the org-parent ref. Defaults to AIP-18's universal-ish 'parent'.").default("parent"), "rules": z.object({ "allowedKinds": z.array(z.string().regex(new RegExp("^[a-z][a-z0-9-]*[a-z0-9]$"))).refine((arr) => arr.every((item, i) => arr.indexOf(item) == i), { message: "All items must be unique!" }).describe("OPTIONAL — collection names that participate in the org tree. Items in collections NOT listed here are outside the tree (e.g. policies, objectives) and must not carry containment refs.").optional(), "allowedParentKinds": z.record(z.string(), z.array(z.string().regex(new RegExp("^[a-z][a-z0-9-]*[a-z0-9]$"))).refine((arr) => arr.every((item, i) => arr.indexOf(item) == i), { message: "All items must be unique!" })).describe("OPTIONAL — containment matrix. Keys are CHILD collection names; values are arrays of allowed PARENT collection names. The host enforces this at item-write time. Per-item violations: office_orgtree_invalid_parent_kind (HARD).").optional(), "maxDepth": z.number().int().gte(1).lte(16).describe("OPTIONAL — maximum org-tree depth. ONE-WAY SWITCH on widening: once set at any ancestor, descendants may narrow (smaller value) but MUST NOT widen. Refusal: office_orgtree_depth_widen (HARD).").optional() }).strict().describe("OPTIONAL containment rules.").optional() }).strict().describe("Org-tree containment axis. AIP-22's distinctive concept: which collection kinds nest under which, how deep the tree goes.").optional(), "reporting": z.object({ "enabled": z.boolean().describe("Whether the reporting graph is active. When false, the host does not validate reportsTo refs.").default(false), "field": z.string().regex(new RegExp("^[a-z][a-zA-Z0-9_]*$")).describe("Item field name carrying the reporting (manager) ref. Distinct from the containment field — a role's manager is logically separate from its containment parent.").default("reportsTo"), "cardinality": z.enum(["single","multiple"]).describe("Reporting cardinality. 'single' (typical) = one manager per role; 'multiple' = matrixed (role MAY report to multiple managers, the field becomes an array).").default("single"), "rules": z.object({ "mustResolveTo": z.string().regex(new RegExp("^[a-z][a-z0-9-]*[a-z0-9]$")).describe("Collection name the report target MUST resolve to. Typically 'role' (a role reports to another role). The host MUST refuse a reportsTo ref pointing at a different kind.").default("role"), "circularBan": z.boolean().describe("Whether reporting cycles are banned. Default true. The host MUST walk the reportsTo chain on every write and refuse a write that would close a cycle (office_orgtree_circular_report, HARD).").default(true) }).strict().describe("Reporting graph constraints.").optional() }).strict().describe("Reporting / authority graph. Logically separate from containment; a role's manager is independent from its containment parent.").optional() }).strict().describe("Org-tree declaration — the AIP-22 distinctive concept. Containment governs the structural tree; reporting governs the authority graph.").optional(), "lints": z.array(z.object({ "id": z.string().regex(new RegExp("^[a-z][a-z0-9-]*$")).describe("Stable kebab-case lint id. Merge key when composing with extends parent."), "kind": z.enum(["orphan-role","broken-report","missing-manager","unassigned-objective","stale-objective","custom"]).describe("Workspace-spanning lint algorithm. AIP-18 per-collection lints (missing-owner, overdue, required-field, etc.) live on COLLECTION.md and are NOT redeclared here. 'custom' delegates to a host-defined check identified by `id`."), "severity": z.enum(["error","warn","info"]).describe("Lint severity. Children may soften; governance policies MAY forbid softening below `error`."), "params": z.record(z.string(), z.any()).describe("Kind-specific parameters. e.g. { collections: [role] } for orphan-role; { days: 30 } for stale-objective.").default({} as never) }).strict()).describe("Workspace-spanning lints. Merge-by-id vs parent.").default([] as never), "defaults": z.object({ "workflow": z.string().min(1).max(512).describe("OPTIONAL — default AIP-15 WORKFLOW.md path or ref. Routine workflow run against company items (e.g. monthly reporting-graph integrity sweep).").optional(), "approvalClass": z.string().regex(new RegExp("^(auto|always|on-mutate|policy:[A-Za-z0-9_./:-]+)$")).describe("Approval class for mutations. 'auto' = no gate; 'always' = every mutation requires approval; 'on-mutate' = approval on field-level mutations; 'policy:<ref>' = delegate to an AIP-7 policy.").optional(), "auditMutations": z.boolean().describe("Whether mutations are audited. ONE-WAY SWITCH: once true at any ancestor, descendants MUST NOT set false. Refusal: office_audit_downgrade (HARD).").default(false) }).strict().describe("Routine workflow defaults. Composes with AIP-15.").optional(), "display": z.object({ "homePage": z.string().regex(new RegExp("^[A-Za-z0-9][A-Za-z0-9_:-]*$")).describe("OPTIONAL — id of the item to use as the company landing page (e.g. a top-level department or the founder role).").optional(), "defaultGrouping": z.enum(["kind","department","parent"]).describe("Default grouping for list views.").optional(), "defaultView": z.enum(["list","tree","board"]).describe("Default rendering mode. 'tree' is the typical pick — org charts render naturally as trees.").optional() }).strict().describe("Display hints for UIs that render the company. Runtime-agnostic.").optional(), "metadata": z.record(z.string(), z.any()).describe("Vendor-specific extensions, namespaced under <vendor>. Deep-merged across the extends chain. MUST NOT change the meaning of any spec field.").default({} as never) }).strict().and(z.any().describe("A view (any manifest with appliesTo set) MUST extend a parent. A workspace-root manifest has neither field.")).describe("Validates the YAML frontmatter portion of an AIP-22 OFFICE.md (workspace root or per-context view). The single doctype 'office.workspace/v1' is used in both modes; the host distinguishes by checking whether `extends` is set. Per-item-kind schemas are delegated to AIP-18 (COLLECTION.md / ITEM.md).")

export type OfficeFrontmatter = z.infer<typeof officeFrontmatterSchema>
