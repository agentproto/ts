# agentproto

This is a **placeholder** package reserving the unscoped `agentproto` name.

The real CLI lives at **[`@agentproto/cli`](https://www.npmjs.com/package/@agentproto/cli)**:

```bash
npm install -g @agentproto/cli
```

That installs the `agentproto` binary on your `PATH`.

— [agentproto.sh](https://agentproto.sh)
