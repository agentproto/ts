/**
 * AIP-20 WORK.md frontmatter zod schema.
 *
 * Generated from `resources/aip-20/draft/WORK.schema.json` via
 * json-schema-to-zod. Imported by both `define-work.ts` (TS path
 * validation) and `manifest/index.ts` (.md path validation) so every
 * field-level constraint runs in both authoring paths from a single
 * source of truth — re-run scaffold-aip to refresh after spec changes.
 *
 * Cross-field rules (if/then/allOf in JSON Schema) don't translate
 * cleanly and live in `define-work.ts`'s `validate(def)` instead.
 */

import { z } from "zod"

export const workFrontmatterSchema = z.object({ "schema": z.literal("work.workspace/v2").describe("Discriminator for the AIP-20 workspace doctype."), "name": z.string().regex(new RegExp("^[a-z][a-z0-9-]*[a-z0-9]$")).min(2).max(96).describe("Stable kebab-case identifier for the workspace or view."), "title": z.string().min(1).max(200).describe("Human-readable workspace title."), "description": z.string().min(1).max(2000).describe("One-paragraph statement of purpose: what this workspace tracks and who uses it."), "version": z.string().regex(new RegExp("^[0-9]+\\.[0-9]+\\.[0-9]+(-[A-Za-z0-9.-]+)?$")).describe("Semantic version of the WORKSPACE shape. Bump on collection / scope / rollup / lint / defaults changes. Independent of the tracker's content version."), "extends": z.string().regex(new RegExp("^(\\.\\./|\\./)[^\\s]+/WORK\\.md$")).min(1).max(512).describe("OPTIONAL — relative path to a parent WORK.md. Presence makes the manifest a VIEW; absence makes it a WORKSPACE ROOT. Recursive composition; maximum chain depth is 8.").optional(), "appliesTo": z.array(z.string().regex(new RegExp("^(ws://(operators|companies|skills)/[a-z][a-z0-9-]*|\\.\\./[^\\s]+)$")).describe("Either a ws:// ref to an AIP-9 operator / AIP-6 company / AIP-3 skill, or a relative path to a consumer workspace folder.")).refine((arr) => arr.every((item, i) => arr.indexOf(item) == i), { message: "All items must be unique!" }).describe("OPTIONAL — list of consumers this VIEW adapts the workspace for. Hosts MUST refuse the view if any binding does not resolve. Not inherited; views declare their own scope.").optional(), "executor": z.string().regex(new RegExp("^ws://operators/[a-z][a-z0-9-]*$")).describe("OPTIONAL — AIP-9 default executor operator. The host activates this operator when an item with no explicit assignee surfaces.").optional(), "governance": z.string().min(1).max(512).describe("OPTIONAL — AIP-7 policy or audit binding. May be a path to an AIP-7 policy file or a ws:// ref. Status-transition approvals, owner-change audits, and scope-widening interventions flow through this ref.").optional(), "knowledge": z.string().regex(new RegExp("^ws://wikis/[a-z][a-z0-9-]*(/KNOWLEDGE\\.md)?$")).describe("OPTIONAL — AIP-10 KNOWLEDGE.md ref. Lets cross-references on items resolve against the bound wiki by default.").optional(), "agency": z.string().regex(new RegExp("^ws://agencies/[a-z][a-z0-9-]*$")).describe("OPTIONAL — AIP-8 agency context. Binds the workspace to an autonomous-agency engagement for billable work, time-tracking, and contractual approval.").optional(), "playbook": z.string().regex(new RegExp("^ws://playbooks/[a-z][a-z0-9-]*$")).describe("OPTIONAL — AIP-12 active playbook. Governs the routine plays this workspace runs.").optional(), "collections": z.array(z.any()).describe("Collections enabled by this workspace. Three forms supported: inline (full COLLECTION.md frontmatter), file ref, or registry import. Merge-by-effective-name (alias if set, otherwise the collection's name) across the extends chain.").default([] as never), "scope": z.object({ "containment": z.object({ "enabled": z.boolean().describe("Whether the containment axis is active. ONE-WAY SWITCH: once true at any ancestor, descendants MUST NOT set false (would orphan items). Refusal: work_scope_disable (HARD).").default(true), "field": z.string().regex(new RegExp("^[a-z][a-zA-Z0-9_]*$")).describe("Item field name carrying the containment ref. Defaults to AIP-18's universal-ish 'parent'.").default("parent"), "rules": z.object({ "allowedKinds": z.array(z.string().regex(new RegExp("^[a-z][a-z0-9-]*[a-z0-9]$"))).refine((arr) => arr.every((item, i) => arr.indexOf(item) == i), { message: "All items must be unique!" }).describe("OPTIONAL — collection names allowed as containment parents. The host enforces this at item-write time.").optional(), "maxDepth": z.number().int().gte(1).lte(32).describe("OPTIONAL — maximum containment depth. The host counts ancestors and refuses items past the cap.").optional() }).strict().describe("OPTIONAL containment constraints.").optional() }).strict().describe("Containment axis (parent/child). Per-collection schemas may declare their own field name; this declaration is the workspace-level default.").optional(), "applicability": z.object({ "enabled": z.boolean().describe("Whether the applicability axis is active.").default(false), "field": z.string().regex(new RegExp("^[a-z][a-zA-Z0-9_]*$")).describe("Item field name carrying the applicability ref list.").default("appliesTo"), "valueClass": z.string().regex(new RegExp("^[a-z][a-z0-9-]*$")).describe("Class of refs the field accepts. Recognised classes: company, role, role-and-company, operator. ONE-WAY SWITCH: once set at any ancestor, descendants MUST NOT change (would invalidate existing items). Refusal: work_scope_value_class_drift (HARD).").optional() }).strict().describe("Applicability axis (who the item is about / scoped to).").optional(), "ownership": z.object({ "enabled": z.boolean().describe("Whether the ownership axis is active.").default(true), "field": z.string().regex(new RegExp("^[a-z][a-zA-Z0-9_]*$")).describe("Workspace-level default item field name carrying the ownership ref. Per-collection AIP-18 schemas may override this with their own ownership.role.").default("owner"), "policy": z.enum(["strict","inherit","open"]).describe("Workspace-level ownership policy. 'strict' requires every collection's ownership.required to be true; 'inherit' delegates to the collection's own setting; 'open' permits any collection to omit ownership.").default("inherit") }).strict().describe("Ownership axis (who is doing the item). Composes with AIP-18 per-collection ownership rules.").optional() }).strict().describe("Workspace-level scope axes — three orthogonal axes (containment / applicability / ownership) declared once per workspace, applied uniformly across enabled collections.").optional(), "statusRollup": z.object({ "enabled": z.boolean().describe("Whether status rollup is active. When false, parent-item statuses are read directly from the item's stored status.").default(false), "policy": z.array(z.object({ "when": z.string().regex(new RegExp("^(all-children-terminal|any-child-blocked|any-child-overdue|no-children|custom:[a-z][a-z0-9-]*)$")).describe("Rollup predicate. Recognised values: all-children-terminal, any-child-blocked, any-child-overdue, no-children, custom:<id>. Merge key when composing."), "bubbleParentStatus": z.string().regex(new RegExp("^[a-z][a-z0-9-]*$")).describe("Status id to surface on the parent when the predicate holds. MUST exist in every collection eligible to be a parent (per scope.containment.rules.allowedKinds).") }).strict()).describe("Rollup rules. Merge-by-`when` against the extends parent.").default([] as never), "exposeViaField": z.string().regex(new RegExp("^[a-z][a-zA-Z0-9_]*$")).describe("OPTIONAL — item field name on parents to materialize the rolled status into. When unset, rollup is query-time only and never written to disk.").optional() }).strict().describe("Status-rollup policy. Per-collection statuses live on AIP-18; this declaration is the workspace-level aggregation layer.").optional(), "lints": z.array(z.object({ "id": z.string().regex(new RegExp("^[a-z][a-z0-9-]*$")).describe("Stable kebab-case lint id. Merge key when composing with extends parent."), "kind": z.enum(["orphan-across-collections","stale-tree","broken-parent-ref","scope-mismatch","custom"]).describe("Workspace-spanning lint algorithm. AIP-18 per-collection lints (missing-owner, overdue, required-field, etc.) live on COLLECTION.md and are NOT redeclared here. 'custom' delegates to a host-defined check identified by `id`."), "severity": z.enum(["error","warn","info"]).describe("Lint severity. Children may soften; governance policies MAY forbid softening below `error`."), "params": z.record(z.string(), z.any()).describe("Kind-specific parameters. e.g. { days: 14 } for stale-tree; { axis: applicability } for scope-mismatch.").default({} as never) }).strict()).describe("Workspace-spanning lints. Merge-by-id vs parent.").default([] as never), "defaults": z.object({ "workflow": z.string().min(1).max(512).describe("OPTIONAL — default AIP-15 WORKFLOW.md path or ref. Routine workflow run against work items in this workspace.").optional(), "approvalClass": z.string().regex(new RegExp("^(auto|always|on-mutate|policy:[A-Za-z0-9_./:-]+)$")).describe("Approval class for mutations. 'auto' = no gate; 'always' = every mutation requires approval; 'on-mutate' = approval on field-level mutations; 'policy:<ref>' = delegate to an AIP-7 policy.").optional(), "auditMutations": z.boolean().describe("Whether mutations are audited. ONE-WAY SWITCH: once true at any ancestor, descendants MUST NOT set false. Refusal: work_audit_downgrade (HARD).").default(false) }).strict().describe("Routine workflow defaults. Composes with AIP-15.").optional(), "display": z.object({ "homePage": z.string().regex(new RegExp("^[A-Za-z0-9][A-Za-z0-9_:-]*$")).describe("OPTIONAL — id of the item to use as the workspace landing page.").optional(), "defaultGrouping": z.enum(["kind","status","owner","parent"]).describe("Default grouping for list views.").optional(), "defaultView": z.enum(["list","board","tree","timeline"]).describe("Default rendering mode.").optional() }).strict().describe("Display hints for UIs that render the workspace. Runtime-agnostic.").optional(), "metadata": z.record(z.string(), z.any()).describe("Vendor-specific extensions, namespaced under <vendor>. Deep-merged across the extends chain. MUST NOT change the meaning of any spec field.").default({} as never) }).strict().and(z.any().describe("A view (any manifest with appliesTo set) MUST extend a parent. A workspace-root manifest has neither field.")).describe("Validates the YAML frontmatter portion of an AIP-20 WORK.md (workspace root or per-context view). The single doctype 'work.workspace/v2' is used in both modes; the host distinguishes by checking whether `extends` is set. Per-item-kind schemas are delegated to AIP-18 (COLLECTION.md / ITEM.md).")

export type WorkFrontmatter = z.infer<typeof workFrontmatterSchema>
