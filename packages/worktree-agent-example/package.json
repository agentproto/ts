{
  "name": "@agentproto/worktree-agent-example",
  "version": "0.0.12",
  "description": "Example: a code-defined AIP-15 workflow that provisions a git worktree, runs a coding agent in it (AgentStep.cwd bound to the provisioned worktree), gates the result, waits for human approval, then cleans up.",
  "private": true,
  "license": "Apache-2.0",
  "type": "module",
  "main": "dist/index.mjs",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsup",
    "clean": "rm -rf dist",
    "check-types": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@agentproto/worktree": "workspace:*"
  },
  "devDependencies": {
    "@agentproto/tooling": "workspace:*",
    "@types/node": "^25.6.2",
    "tsup": "^8.5.1",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
