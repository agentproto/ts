# `@agentproto/adapter-kit` (deprecated)

This package has been renamed to `@agentproto/provider-kit`.

`@agentproto/adapter-kit@0.3.0` is a thin compatibility shim that re-exports `@agentproto/provider-kit` unchanged. New code should import from `@agentproto/provider-kit`.
