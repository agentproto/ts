/**
 * `@agentproto/app-client/react` — TanStack Query hooks over the bridge
 * client. Implementation lands in the app-client slice; this stub only pins
 * the entry point so the skeleton builds.
 */

export const APP_CLIENT_REACT_PLACEHOLDER = true
