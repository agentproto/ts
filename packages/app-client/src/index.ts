/**
 * `@agentproto/app-client` — framework-free typed client for the
 * `window.McpApp` bridge. Implementation lands in the app-client slice;
 * this stub only pins the entry point so the skeleton builds.
 */

export const APP_CLIENT_PLACEHOLDER = true
