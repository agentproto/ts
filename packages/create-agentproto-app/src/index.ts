/**
 * `create-agentproto-app <dir>` — scaffold an agentproto agent app.
 * Implementation lands in the scaffolder slice; this stub only pins the
 * entry point so the skeleton builds.
 */

export const CREATE_APP_PLACEHOLDER = true
