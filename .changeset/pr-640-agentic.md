---
agentproto-vscode: minor
---

Add "Use My Existing Local Login" — source-backed auth profiles that reuse local CLI logins (Claude Code, Codex, Gemini) without pasting tokens. Tokens are resolved fresh on every spawn, with optional activation-time auto-adopt policy.
