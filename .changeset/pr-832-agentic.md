---
"agentproto-vscode": patch
---

Add GFM pipe table support to the transcript markdown renderer. Tables support alignment markers, inline formatting inside cells, and properly handle escaped pipes and pipes inside code spans.
