---
"@agentproto/runtime": minor
---

Surface `addedAt` ISO date field on `CatalogProviderModel` interface to expose model introduction timestamps in provider model enumeration, enabling "new" badges in catalog pickers.
