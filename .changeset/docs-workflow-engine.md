---
"@agentproto/workflow-runtime": patch
---

Add a package README documenting the harness-parity engine — the typed step algebra plus worked examples for engine unification, structured output (schema + re-prompt), aggregate budget (`maxTotalCostUsd`), pipeline mode (no cross-item barrier), and the resume cache — alongside a new CLI-docs Workflows concept page covering the `workflow_start` / `workflow_status` MCP surface and how it maps onto this engine.
