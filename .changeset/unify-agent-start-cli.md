---
"@agentproto/cli": minor
---

sessions start: add --orchestrator, --orchestrator-json and --mcp-servers-json flags so the CLI can spawn orchestrator-enabled sessions and inject mcpServers, reaching parity with the agent_start MCP tool.
