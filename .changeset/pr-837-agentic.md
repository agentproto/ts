---
agentproto-vscode: patch
---

Fix markdown block rendering in conversation book by adding CSS for structural elements (paragraphs, lists, code fences, blockquotes, headings, tables) and correct chevron alignment.
