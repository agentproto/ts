---
"agentproto-vscode": minor
---

Add support for user-saved presets in the VS Code extension. Users can now quickly spawn sessions using frequently-used configurations saved to ~/.agentproto/presets.json, with preset selection integrated directly into the spawn quick-pick menu. Includes graceful degradation on older daemons and comprehensive error handling.
