---
"@agentproto/llm-endpoint": minor
---

Surface verified context window and max output token limits in model list responses via optional `contextWindow` and `maxOutputTokens` fields on `ModelRoute`.
