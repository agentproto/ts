---
agentproto-vscode: patch
---

Route child sessions (with parentSessionId) to the auto lane's Tasks group instead of nesting them under their parent. Display adapter SVG icons instead of text slugs in the composer bar for improved UX.
