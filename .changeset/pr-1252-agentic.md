---
"agentproto-vscode": minor
---

Add a `chat-panel` session view: a new `agentproto.openSessionInChatPanel` command and a `chat-panel` option for `agentproto.sessionView` that opens sessions in a webview panel embedding the Session Chat app in an iframe (with a CSP pinned to the daemon origin).
