---
"agentproto-vscode": patch
---

Webview sessions list now uses the tree's fine-grained `activityFor` classifier to distinguish working/idle/stalled sessions and provide more granular filtering options, eliminating disagreement between views.
