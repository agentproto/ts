---
"@agentproto/corpus": minor
---

Declare an optional `sha` content address on knowledge `SourceRef`, so corpus entries whose origins are not fetchable pages can still carry a stable resolution key.
