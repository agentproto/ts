---
---

ci: agent harness v2 — shared agent core, on-demand slash/@mention commands
(`/review`, `/fix [--pr]`, `/pr`, `/implement`, `/explain`, `/help`),
configurable model + skills, stacked-PR fix delivery. CI/scripts only, no
package changes.
