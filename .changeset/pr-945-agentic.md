---
agentproto-vscode: minor
---

Add Auth & Models Explorer — an editable webview for managing auth profiles, wallet curation, and model routing. Features vendor-grouped model allowlists, per-wallet enable/disable, provider connection flows, and local-router upstream link management.
