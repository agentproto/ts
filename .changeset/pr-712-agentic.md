---
"agentproto-vscode": minor
---

Add opt-in Sessions webview panel — a modernized alternative to the tree view with a pinned filter input, status tabs, two-line rows, harness/model glyphs, subagent nesting, and an open-in-tab indicator. Controlled via `agentproto.sessionsView` config (enum: "tree" | "webview", default: "tree").
