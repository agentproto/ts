---
"agentproto-vscode": minor
---

Add terminal location management: users can now toggle terminals between editor and panel views via a new `moveTerminalLocation` command in the terminal title context menu. Integrates a new `openTerminal` webview message type to switch from conversation view to terminal view (WP1–WP3).
