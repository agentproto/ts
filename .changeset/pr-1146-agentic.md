---
"@agentproto/sandbox-e2b": patch
---

Record stable e2b workstation template as proven with real baked CLI and adapters (0.17.0 + hermes/opencode); skip on-boot npm install when template is proven and cliVersion matches, reducing boot time by ~90s on default paths.
