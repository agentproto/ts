---
"@agentproto/runtime": minor
---

Add the `agentproto_session_story` MCP app — a per-session readable timeline panel (mission summary, inferred sub-task chapters, chapter-segmented step feed, anchored detail panel, and composer) built on a new pure `buildStory` heuristic module.
