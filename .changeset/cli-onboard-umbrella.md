---
"@agentproto/cli": minor
---

feat(cli): add `agentproto onboard` — first-run umbrella that registers the daemon MCP server and installs the agentproto skill pack in one pass (wraps install-mcp + install skill; both remain usable standalone)
