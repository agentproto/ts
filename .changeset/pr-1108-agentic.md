---
"@agentproto/workflow-runtime": minor
"@agentproto/apps": patch
---

Add forward-only branch step compilation and subworkflow input projection support to the workflow runtime compiler, plus validation of step references at compile time.
