---
"@agentproto/adapter-code-brain-gbrain": minor
---

New adapter package implementing ICodeBrainProvider against live gbrain code-intelligence backend with local (docker exec) and remote (HTTP/bearer) transports, plus tracked gbrain runtime launcher and ask_codebase MCP server.
