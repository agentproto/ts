---
"agentproto-vscode": patch
---

Remove stale hero element from empty state when real conversation turns arrive. Fixes a bug where the "Ready when you are" message persisted after the first message.
