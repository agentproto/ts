---
"@agentproto/runtime": patch
---

Document `worktrees.isolation` configuration policy with detailed explanation of the three modes (on-request, always, never), depth-0-only behavior for nested spawns, and important config-key gotcha that can lead to silent misconfiguration.
