---
agentproto-vscode: minor
---

Add interactive permission-ask UI with clickable option buttons. Introduces `resolveQuestion` webview message type for permission responses and updates daemon integration to handle structured permission decisions via toolCallId.
