---
agentproto-vscode: minor
---

Add autolinking for URLs and file paths in transcript webview. Bare URLs (http/https/file), markdown-style links, and file paths with `:line` citations are now clickable — external URLs open in the browser, file paths open in the editor.
