---
"@agentproto/cli": minor
---

Centralize session watch keypress handling and add a `s` key to view a session's Story/conversation. Exposes new public helpers `isTerminalSession`, `sessionRowLabel`, `terminalKindMark`, `attachMode`, `decodeWatchKey`, and the `WatchKeyAction` type.
