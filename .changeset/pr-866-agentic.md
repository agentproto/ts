---
"@agentproto/app-kit": minor
"@agentproto/runtime": minor
---

Add UI, artifacts, and dev-launch configuration support to app-kit. Apps can now declare HTML surfaces, artifact types, and dev-launch configurations that are carried through emit/load and integrated into the runtime app registry.
