---
"@agentproto/cli": minor
---

add `mcp-bridge` stdio command — a stdio MCP server that proxies the daemon HTTP `/mcp` endpoint (forwarding real tool schemas) so stdio-only clients (Codex, Cursor, Claude Desktop, standalone Hermes) can use agentproto tools without a third-party bridge.
