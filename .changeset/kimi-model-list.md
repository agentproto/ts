---
"@agentproto/cli": patch
---

Declare a model list on the kimi-cli generic-ACP spec (default kimi-k3 plus
the moonshot allow-list) so its launch picker offers real models instead of
only "custom".
