---
"@agentproto/runtime": minor
"@agentproto/sandbox": minor
"@agentproto/cli": patch
---

Sandbox spawn defaults cwd to the box's home, kills by default on close, and reconciles the ledger against the real provider
