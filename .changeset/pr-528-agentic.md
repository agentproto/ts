---
"agentproto-vscode": minor
---

Add per-window workspace pinning feature to prevent the daemon's global `active` workspace from being mutated across VS Code windows. Includes new `agentproto.selectWorkspace` command, status bar indicator, and spawn wizard integration.
