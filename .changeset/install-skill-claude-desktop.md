---
"@agentproto/cli": minor
---

Add a `claude-desktop` target to `agentproto install skill` — installs a skill into
Claude Desktop's local-agent skills bundle and upserts its `manifest.json` registry
entry (by name, backing up the manifest first).