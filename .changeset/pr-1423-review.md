---
"@agentproto/runtime": minor
"@agentproto/workflow-runtime": patch
"@agentproto/cli": minor
"@agentproto/apps": patch
---

Mount daemon /mcp gateway on workflow agent steps; fix prompt sections, map scheduling, run output persistence, maintain --wait
