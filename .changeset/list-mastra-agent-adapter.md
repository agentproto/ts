---
"@agentproto/cli": patch
---

docs: list the first-party `mastra-agent` adapter alongside the others.

It was registered in the CLI catalog (and live-spawnable) but missing from the
adapter enumerations in the README, the getting-started walkthrough, and the
bundled agentproto skill. Add it to all three so the first-party agent is
discoverable.
