---
agentproto-vscode: minor
---

Add support for task/child sessions in webview grouping. Sessions with `parentSessionId` are now routed to a new "Tasks" subgroup in the auto lane. Also improve SVG rendering for adapter icons in the composer.
