---
"@agentproto/sandbox": minor
"@agentproto/sandbox-e2b": minor
---

Add createSandboxAgentSessionHost (@agentproto/sandbox) and an e2b SandboxProvider (@agentproto/sandbox-e2b) — run any AgentStep's coding-agent turn inside an e2b sandbox by pointing the existing daemon-backed AgentSessionHost at the sandboxed daemon's MCP URL.
