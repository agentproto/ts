---
"@agentproto/catalog-sync": patch
"agentproto-vscode": patch
---

image:replicate generator now accepts both DRF-paginated (`results`) and hand-authored snapshot (`models`) shapes via zod validation; restore @types/vscode engines-floor pin (1.90.0).
