---
"@agentproto/apps": minor
---

Add the `@agentproto/config` read-only MCP app (wallets, harnesses, models, defaults, remote, advanced) with a new `./config` subpath export, the exported `configApp` handle, and the deep-link fragment helpers `parseConfigFragment`/`buildConfigFragment`/`CONFIG_SECTIONS`.
