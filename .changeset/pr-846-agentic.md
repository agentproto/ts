---
agentproto-vscode: patch
---

Replace stop-lookalike glyph with archive-box SVG icon in the Sessions webview archived toggle button. The ▣ character was visually similar to the stop button (square in circle), causing user confusion. Now uses the proper ARCH_SVG icon instead.
