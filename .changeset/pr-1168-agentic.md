---
"@agentproto/workflow": minor
"@agentproto/workflow-loader": patch
"@agentproto/workflow-runtime": patch
---

Extract shared `$steps.<id>` reference validation logic into a reusable utility in @agentproto/workflow, eliminating duplication across workflow-loader and workflow-runtime.
