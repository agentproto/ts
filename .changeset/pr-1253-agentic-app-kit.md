---
"@agentproto/app-kit": minor
---

Add an optional `csp.frameDomains` field to app frontmatter/UI types so apps can declare trusted embedding origins; forwarded into the installed-app UI definitions.
