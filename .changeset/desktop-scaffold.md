---
"agentproto-desktop": minor
---

Scaffold agentproto-desktop — a native desktop shell (Tauri v2 + React) over the agentproto daemon, with a Rust core exposing daemon_health / daemon_sessions and a first live session-list screen.
