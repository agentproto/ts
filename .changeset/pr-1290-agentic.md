---
"@agentproto/command-sandbox": patch
"agentproto-vscode": patch
---

Test-only: add runtime capability probes (Seatbelt nesting, loopback bind) so environment-dependent suites skip with a clear reason instead of failing in confined environments.
