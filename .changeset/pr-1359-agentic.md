---
"@agentproto/catalog-sync": patch
---

Filter the image:replicate generator to curated models only; fail fast on roster/API-shape drift. Tests are now shape-independent (in-memory payloads instead of fixture round-trips).
