---
"agentproto-desktop": minor
---

Desktop slice 3 — a Files tree tab (Rust `list_dir` command + `FilesPanel`) and a drag-to-resize left rail. The rail width flows through a `--rail-w` CSS custom property so the responsive `@media` breakpoints still reclaim the layout as the window narrows.
