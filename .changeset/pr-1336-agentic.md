---
"@agentproto/skill-pack-agentpush": patch
---

Sync the agentpush SKILL.md `transmit_message` return-shape docs with runtime behavior (success/failure shapes, `message_id`, `blocked_reason`/`suggestion` for HTTP-200 blocked/failed statuses).
