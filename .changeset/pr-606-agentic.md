---
"agentproto-vscode": minor
---

Add comprehensive auth profile management: split presets into connected/unconnected states with one-click connect, show models each wallet can bill, and enhance spawn wizard to properly handle gateway models with explicit route + access resolution.
