---
"@agentproto/cli": minor
---

Add `agentproto install skill/<slug>` — install skills into hermes (`~/.hermes/skills/`) and Claude Code (a packaged plugin bundle), with an overwrite prompt, `--dry-run`, `--list`, and `agentproto-pack` to install the whole pack.
