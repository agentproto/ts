---
"@agentproto/runtime": minor
"@agentproto/sandbox": minor
"@agentproto/cli": minor
---

Sandbox orphan reaping (`sandbox gc` CLI, `collectGcCandidates`/`reapGcEntry`), `SandboxHostBootFailedError` with pause-first teardown on failed daemon connect, `GET /sessions/:id/alive` liveness endpoint + `alive` descriptor field, token gates on `/mcps/imports` and `/mcps/proxy/call`, worktree provisioning defaults to async, and a new `agentproto workflow` CLI verb.