---
"agentproto-vscode": minor
---

Add optional webview-based panels for Harnesses and Auth Profiles as alternatives to TreeView panels, with adapter logos, inline actions, and collapsible sections. Both are opt-in via configuration and default to the existing tree views for backward compatibility.
