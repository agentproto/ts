---
"agentproto-vscode": minor
---

Adds a compact read-only Work sidebar webview (`agentproto.work`) over the daemon's Task ledger, plus an `agentproto.openWorkBoard` launcher command that opens the installed `@agentproto/work-board` app via the shared app-panels host.
