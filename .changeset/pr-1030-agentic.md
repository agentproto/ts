---
"@agentproto/runtime": minor
"@agentproto/cli": minor
"agentproto-vscode": minor
---

Extract release-check logic from VS Code into `@agentproto/runtime` for code sharing with the CLI. Add `daemon status` release indicator and VS Code update-prompt command with tarball/workspace-specific behaviors.
