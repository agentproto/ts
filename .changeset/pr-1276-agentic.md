---
"agentproto-vscode": patch
---

Add a Refresh control to the Activity webview footer (posting the already-handled `refresh` webview-to-host message) and add DOM-level jsdom coverage that executes the real `buildHtml` output.