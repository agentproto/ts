---
agentproto-vscode: patch
---

Refactor webview logo rendering: replace `harnessGlyph` string with structured `AdapterLogo` type supporting both icon files and lettermark fallbacks, enabling richer visual branding for different adapter providers.
