---
"agentproto-vscode": minor
---

Add `agentproto.openSessionInChat` command and `agentproto.sessionView` setting (`chat` | `builtin`), routing session opens to the daemon's Session Chat app UI with silent fallback to the builtin transcript panel.