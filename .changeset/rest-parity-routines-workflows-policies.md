---
"@agentproto/runtime": minor
---

Add REST parity for routines, workflows, and policies — HTTP routes for start/list/get/cancel/escalation-resolve (routines, workflows) and start/list/cancel/ack (policies), mirroring the existing MCP tool surfaces over the same underlying runner instances.
