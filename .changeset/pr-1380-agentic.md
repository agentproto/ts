---
"@agentproto/adapter-claude-code": patch
"@agentproto/adapter-claude-sdk": patch
---

Bump pinned ACP wrapper `@agentclientprotocol/claude-agent-acp` to 0.81.2 and `@anthropic-ai/claude-agent-sdk` to ^0.3.282 so `claude-opus-5-5` can spawn (requires Claude Code >= 2.1.280); sync `CLAUDE-CODE.md` manifest with the new pin.
