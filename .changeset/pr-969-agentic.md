---
"@agentproto/adapter-grok-cli": minor
---

Add native ACP adapter for xAI's Grok Build CLI (`grok agent stdio`) with verified model list, auth configuration, and capabilities matching live grok 1.0.3 ACP handshake.
