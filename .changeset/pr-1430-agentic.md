---
"@agentproto/skill-pack-agentproto": patch
---

Docs: document inter-session messaging (`inbox_wait` supervision loop, `message_send`/`message_reply`/`inbox_*` tools, `messaging` config block) in `ap-prompt-agent` and `pb-supervise-parallel-mission` skills.
