---
"@agentproto/workflow-runtime": minor
"@agentproto/runtime": minor
---

Add an `AgentStep` kind to `workflow-runtime` and run `WorkflowRunner` on the shared interpreter via a host-injected `AgentSessionHost`. `workflow_start` / `workflow_status` behavior and schemas are unchanged.
