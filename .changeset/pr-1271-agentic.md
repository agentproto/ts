---
"agentproto-vscode": minor
---

Add a read-only Activity sidebar webview (`agentproto.activity`) showing the daemon's Activity projection plus PTY terminals and raw command sessions; the Sessions panel no longer carries shell sessions.
