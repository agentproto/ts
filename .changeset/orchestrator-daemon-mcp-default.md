---
"@agentproto/runtime": patch
---

Default-mount daemon MCP gateway for hermes agent_start spawns issued through the scoped `/mcp/orchestrator` sub-gateway (completes #138's fix for the recursive-orchestrator path)
