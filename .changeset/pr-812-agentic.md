---
"@agentproto/adapter-mastra-agent": minor
---

Add three new git-backed workspace tools (read_diff, apply_patch, run_tests) with security validation (path escaping, command whitelisting), and introduce a pluggable extraTools mechanism for hosts to extend the toolset.
